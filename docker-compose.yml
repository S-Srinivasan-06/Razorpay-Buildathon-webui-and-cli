version: '3.8'
services:
  recon-agent:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      # Persist audit trails and uploads across container restarts
      - recon_data:/app/data
    restart: unless-stopped

volumes:
  recon_data:
